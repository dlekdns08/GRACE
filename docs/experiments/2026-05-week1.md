# 2026-05 Week 1

## Bootstrap log

- 2026-05-05: Initial scaffold of all 8 phases. README + DESIGN.md + skeleton complete.
  - Phase 0: pyproject.toml (uv-compatible), `.gitignore`, package layout.
  - Phase 1: `src/llm/{client,mock,cache,async_client,parsers,prompts}.py` + tests.
  - Phase 2: `src/envs/{base,state_text,python_env,dummy_env}.py` + tests.
  - Phase 3: PPO trainer + rollout + train.py entry point.
  - Phase 4: LLM-augmented policy + heuristic meta-policies (FixedK, Entropy, Never).
  - Phase 5: Learned meta-policy MLP + GRPO trainer.
  - Phase 6: Unity C# scaffolding (KitchenEnvironment, ChefAgent, PotController, StateSerializer).
  - Phase 7: eval.py, sweep.py, plot_results.py.

## Decisions made (autonomous)

1. Use `overcooked-ai-py` (Carroll's) via git install for Python env.
2. Multi-agent self-play with shared policy.
3. GRPO group = K rollouts from same init state, different action seeds.
4. Sequential learning: train PPO first, then meta-policy on top of frozen policy.
5. CI deferred. W&B optional (default disabled).

## Open questions / TODO

- Verify Carroll's API surface against the version pinned in pyproject.toml — schema may change between releases.
- Tune `call_cost` (λ) for GRPO via sweep.
- Decide whether to include action history in `LearnedMetaPolicy` features.
